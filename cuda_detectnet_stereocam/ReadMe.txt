================================================================================
Author: Beneder Roman
Version: v0.1
Date: 28.03.2022
================================================================================
================================================================================
v0.1:
.) connect stereo camera
.) open a terminal within the root-dir of this project
.) check if PATH variable contains "/usr/local/cuda-10.2/bin"
.) if not add path to PATH variable with command "export PATH=$PATH:/usr/local/cuda-10.2/bin"
.) check updated PATH variable with command "echo $PATH"
.) if successfully added execute command "make -j4"
.) execute application with "./app_realsense"

